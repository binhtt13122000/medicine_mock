export enum ROUTES {
  //Routes Page
  DASHBOARD = "/dashboard",
  DRUG = "/drug",
  ORDER = "/order",
  STORAGE = "/storage",
  ACCOUNT = "/d",
  WORKSESSION = "/worksession",
  SHIFT = "/shift",
  VOID_REASON = "/void-reason",
  LOCATION = "/location",
  TABLE = "/table",
}
